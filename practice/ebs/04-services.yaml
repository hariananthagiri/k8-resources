apiVersion: v1
kind: Service
metadata:
  name: ebs-static
spec:
  type: LoadBalancer
  selector:
    demo: demo-label
  ports:
  - protocol: TCP
    port: 80 #service-port
    targetPort: 80 #container-port
    



