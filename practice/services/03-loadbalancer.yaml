apiVersion: v1
kind: Service
metadata:
  name: <service-name>
  namespace: roboshop1
spec:
  type: LoadBalancer
  selector:
    app: nginx
    demo: service
  ports:
  - protocol: TCP
    port: 80 #service-port
    targetPort: 80 #container-port